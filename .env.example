# ── App ───────────────────────────────────────────────────
APP_NAME=EcommerceAPI
DEBUG=false
ENVIRONMENT=development  # development, staging, production

# ── Security ──────────────────────────────────────────────
# CRITICAL: Generate a secure random key with: openssl rand -hex 32
# NEVER use the example key below in production!
SECRET_KEY=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_HEX_32_MINIMUM_32_CHARS
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7
ACCESS_TOKEN_COOKIE_NAME=access_token
REFRESH_TOKEN_COOKIE_NAME=refresh_token
COOKIE_SECURE=false
COOKIE_SAMESITE=lax
COOKIE_DOMAIN=

# ── API Documentation Protection ──────────────────────────
# Required in production to protect Swagger/ReDoc endpoints
DOCS_USERNAME=admin
DOCS_PASSWORD=your_secure_docs_password

# ── Redis ─────────────────────────────────────────────────
# Leave empty to disable Redis (caching will be disabled)
REDIS_URL=redis://localhost:6379/0

# ── CORS ──────────────────────────────────────────────────
# Specify exact origins, never use "*" in production
ALLOWED_ORIGINS=["http://localhost:3000","https://yourdomain.com"]

# ── Stripe ────────────────────────────────────────────────
STRIPE_SECRET_KEY=sk_test_your_stripe_key

# ── SendGrid ──────────────────────────────────────────────
SENDGRID_API_KEY=your_sendgrid_api_key

# ── AWS ───────────────────────────────────────────────────
AWS_ACCESS_KEY_ID=your_aws_key
AWS_SECRET_ACCESS_KEY=your_aws_secret
AWS_S3_BUCKET=your-bucket-name
AWS_REGION=us-east-1

# ──────────────────────────────────────────────────────────
# SECURITY CHECKLIST FOR PRODUCTION:
# ──────────────────────────────────────────────────────────
# [ ] Set ENVIRONMENT=production
# [ ] Generate SECRET_KEY with: openssl rand -hex 32
# [ ] Set strong DOCS_PASSWORD
# [ ] Configure REDIS_URL for production Redis instance
# [ ] Set exact ALLOWED_ORIGINS (no wildcards)
# [ ] Use production Stripe key (sk_live_...)
# [ ] Configure all external service credentials
# [ ] Never commit .env file to git (use .env.example only)
# ──────────────────────────────────────────────────────────
